const ProofStrip = () => {
  return (
    <div className='proof-strip'>
      <p>
        Most recently: designed and scaled a construction-operations platform — scoping, bidding, and real-time field work — now running across multiple states inside a profitable business.
      </p>
    </div>
  );
};

export default ProofStrip;
